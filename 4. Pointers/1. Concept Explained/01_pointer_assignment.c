#include<stdio.h>
#include<conio.h>

int main()
{
	int a=10,b=9;
	int *p,*q;			// p and q got capacity to store address values

	p = &a;
	q= &b;

	printf("Address of a: %X",p);
	pirntf("Address of a: %X",)
	return 0;
}